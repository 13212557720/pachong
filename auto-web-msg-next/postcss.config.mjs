/**
 * Tailwind CSS v4 PostCSS 配置文件
 *
 * 此文件用于配置 PostCSS 以支持 Tailwind CSS v4 的使用。
 * Tailwind CSS v4 使用 @tailwindcss/postcss 作为 PostCSS 插件。
 *
 * @module postcss.config
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;